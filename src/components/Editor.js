import React, { useEffect } from "react";

const Editor = () => {
  useEffect(() => {
    async function init() {
      
    }
    init();
  }, []);
  
  return <textarea id="realtimeEditor"></textarea>
};

export default Editor;
